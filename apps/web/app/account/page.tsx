'use client'

import React from 'react'
import WithSidebar from '../../components/sidebar'

const Account = () => {
  return (
    <div>
      Account
    </div>
  )
}

export default WithSidebar(Account)
